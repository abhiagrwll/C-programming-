//Reverse of string
#include <stdio.h>
#include <string.h>
int main()
{
    char st[100];
    int i;
    scanf("%[^\n]",st);
    for(i=strlen(st)-1;i>=0;i--)
        printf("%c",st[i]);
    return 0;
}

