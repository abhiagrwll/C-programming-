//Write a C program to count total number of words in a string.

#include <stdio.h>
int main()
{
    char st[100],i,c=0;
    scanf("%[^\n]",st);
    for(i=0;st[i];i++)
    {
        if(st[i]==32)
        c++;
    }
    printf("no. of words =%d",c+1);
    return 0;
}
